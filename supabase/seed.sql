-- ==============================================================================
-- ECSA PLATFORM SEED DATA (Non-sensitive / Lookups only)
-- ==============================================================================
-- No fake users, clubs, or events are inserted here.

-- Since department, types, and categories are text fields in the current schema, 
-- we do not strictly need lookup tables. If lookup tables are added later, 
-- this is where they would be seeded.

-- Example for reference:
-- INSERT INTO departments (name, code) VALUES 
--   ('Electronics & Telecommunication', 'ENTC'),
--   ('Computer Engineering', 'COMP'),
--   ('Information Technology', 'IT');
